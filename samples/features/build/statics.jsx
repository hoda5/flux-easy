var ContextSample = React.createClass({
    ContextTypes: {
        x: 1
    },

    getInitialState: function getInitialState() {
        var state = {
            v: 1
        };

        return state;
    },

    render: function() {
        return (
            <div>
            </div>
        );
    }
});;
